module.exports = {
  // NOTE: Uncomment this if NextJS doesn't appear to be picking up
  // file changes; it should help.
  // webpackDevMiddleware: (config) => {
  //   config.watchOptions.poll = 300;
  //
  //   return config;
  // }
}
